{
  "name": "chat-backend",
  "version": "1.0.0",
  "main": "server.js",        // <-- main backend file
  "scripts": {
    "start": "node server.js" // <-- Render uses this to run the server
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.1.0",
    "socket.io": "^4.7.2"
  }
}